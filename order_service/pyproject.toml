[project]
name = "order-service"
version = "0.1.0"
description = "Application for serving users orders"
authors = [
    {name = "ol1xy",email = "aleksejserduk1@gmail.com"}
]
license = "MIT"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
